[project]
name = 'Hospital-pharmacy-automated-dispensing-cabinet-auditing'
description = ''
authors = []
telemetry = false
cache_dir = './.cache'
requirements = []
[contracts.adc-audit-coordinator]
path = 'contracts/adc-audit-coordinator.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
